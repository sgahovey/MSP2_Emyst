{% extends 'base.html.twig' %}

{% block title %}Emyst - Votre compagnon fitness{% endblock %}

{% block body %}
<div class="home-container">
    <div class="home-hero">
        <div class="home-logo">
            <div class="home-logo-icon">E</div>
            <h1 class="home-title">Emyst</h1>
        </div>
        <p class="home-subtitle">Votre compagnon pour suivre vos séances d'entraînement et atteindre vos objectifs</p>
        
        <div class="home-features">
            <div class="home-feature-card">
                <div class="home-feature-icon">💪</div>
                <h3>Gérez vos séances</h3>
                <p>Créez et suivez vos entraînements facilement</p>
            </div>
            <div class="home-feature-card">
                <div class="home-feature-icon">🎯</div>
                <h3>Définissez vos objectifs</h3>
                <p>Fixez-vous des objectifs et suivez votre progression</p>
            </div>
            <div class="home-feature-card">
                <div class="home-feature-icon">📊</div>
                <h3>Visualisez vos progrès</h3>
                <p>Consultez vos statistiques et votre évolution</p>
            </div>
        </div>

        <div class="home-actions">
            <a href="{{ path('app_register') }}" class="btn-home btn-home-primary">
                <span>S'inscrire</span>
            </a>
            <a href="{{ path('app_login') }}" class="btn-home btn-home-secondary">
                <span>Se connecter</span>
            </a>
        </div>
    </div>
</div>
{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <style>
        /* Masquer la sidebar pour la page d'accueil */
        body .layout .sidebar {
            display: none !important;
        }

        body .layout {
            display: block !important;
            grid-template-columns: 1fr !important;
        }

        body .content {
            width: 100% !important;
            max-width: none !important;
            margin: 0 !important;
            padding: 0 !important;
            background: transparent !important;
            min-height: auto !important;
        }

        /* Override layout pour la page d'accueil */
        .home-container {
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 40px 24px;
            background: linear-gradient(180deg, #07101a 0%, #081426 50%, #0a1528 100%);
            position: relative;
            overflow: hidden;
        }

        /* Effet de particules animées en arrière-plan */
        .home-container::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: 
                radial-gradient(circle at 20% 30%, rgba(110, 231, 183, 0.1) 0%, transparent 50%),
                radial-gradient(circle at 80% 70%, rgba(96, 165, 250, 0.1) 0%, transparent 50%);
            animation: pulse 8s ease-in-out infinite;
            pointer-events: none;
        }

        @keyframes pulse {
            0%, 100% { opacity: 0.5; }
            50% { opacity: 1; }
        }

        .home-hero {
            max-width: 1100px;
            width: 100%;
            text-align: center;
            position: relative;
            z-index: 1;
        }

        .home-logo {
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 20px;
            margin-bottom: 32px;
            animation: fadeInDown 0.8s ease-out;
        }

        @keyframes fadeInDown {
            from {
                opacity: 0;
                transform: translateY(-30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .home-logo-icon {
            width: 72px;
            height: 72px;
            background: linear-gradient(135deg, var(--accent), var(--accent-2));
            border-radius: 20px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 36px;
            font-weight: 700;
            color: #021;
            box-shadow: 0 8px 32px rgba(110, 231, 183, 0.3);
            animation: float 3s ease-in-out infinite;
            position: relative;
        }

        .home-logo-icon::after {
            content: '';
            position: absolute;
            inset: -2px;
            border-radius: 20px;
            padding: 2px;
            background: linear-gradient(135deg, var(--accent), var(--accent-2));
            -webkit-mask: linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0);
            -webkit-mask-composite: xor;
            mask-composite: exclude;
            opacity: 0;
            animation: glow 2s ease-in-out infinite;
        }

        @keyframes float {
            0%, 100% { transform: translateY(0px); }
            50% { transform: translateY(-10px); }
        }

        @keyframes glow {
            0%, 100% { opacity: 0; }
            50% { opacity: 0.6; }
        }

        .home-title {
            font-size: 56px;
            font-weight: 800;
            color: #ffffff;
            margin: 0;
            letter-spacing: -1px;
            background: linear-gradient(135deg, #ffffff 0%, rgba(255, 255, 255, 0.8) 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            animation: fadeInDown 0.8s ease-out 0.2s both;
        }

        .home-subtitle {
            font-size: 22px;
            color: rgba(255, 255, 255, 0.75);
            margin-bottom: 64px;
            line-height: 1.7;
            font-weight: 300;
            animation: fadeInDown 0.8s ease-out 0.4s both;
        }

        .home-features {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 28px;
            margin-bottom: 56px;
            animation: fadeInUp 0.8s ease-out 0.6s both;
        }

        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .home-feature-card {
            background: rgba(255, 255, 255, 0.04);
            backdrop-filter: blur(20px);
            -webkit-backdrop-filter: blur(20px);
            border: 1px solid rgba(255, 255, 255, 0.08);
            padding: 40px 28px;
            border-radius: 20px;
            transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
            position: relative;
            overflow: hidden;
        }

        .home-feature-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.1), transparent);
            transition: left 0.5s;
        }

        .home-feature-card:hover::before {
            left: 100%;
        }

        .home-feature-card:hover {
            transform: translateY(-8px) scale(1.02);
            background: rgba(255, 255, 255, 0.08);
            border-color: rgba(110, 231, 183, 0.3);
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.3), 0 0 20px rgba(110, 231, 183, 0.1);
        }

        .home-feature-icon {
            font-size: 56px;
            margin-bottom: 20px;
            display: inline-block;
            transition: transform 0.4s cubic-bezier(0.4, 0, 0.2, 1);
        }

        .home-feature-card:hover .home-feature-icon {
            transform: scale(1.1) rotate(5deg);
        }

        .home-feature-card h3 {
            font-size: 20px;
            font-weight: 700;
            color: #ffffff;
            margin-bottom: 12px;
            letter-spacing: -0.5px;
        }

        .home-feature-card p {
            font-size: 15px;
            color: rgba(255, 255, 255, 0.65);
            margin: 0;
            line-height: 1.6;
        }

        .home-actions {
            display: flex;
            gap: 20px;
            justify-content: center;
            flex-wrap: wrap;
            animation: fadeInUp 0.8s ease-out 0.8s both;
        }

        .btn-home {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            padding: 16px 40px;
            border-radius: 12px;
            font-size: 17px;
            font-weight: 600;
            text-decoration: none;
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            border: none;
            cursor: pointer;
            position: relative;
            overflow: hidden;
            letter-spacing: 0.5px;
        }

        .btn-home::before {
            content: '';
            position: absolute;
            top: 50%;
            left: 50%;
            width: 0;
            height: 0;
            border-radius: 50%;
            background: rgba(255, 255, 255, 0.2);
            transform: translate(-50%, -50%);
            transition: width 0.6s, height 0.6s;
        }

        .btn-home:hover::before {
            width: 300px;
            height: 300px;
        }

        .btn-home-primary {
            background: linear-gradient(135deg, var(--accent), var(--accent-2));
            color: #021;
            box-shadow: 0 4px 20px rgba(110, 231, 183, 0.3);
        }

        .btn-home-primary:hover {
            transform: translateY(-3px) scale(1.05);
            box-shadow: 0 12px 40px rgba(110, 231, 183, 0.5);
        }

        .btn-home-primary:active {
            transform: translateY(-1px) scale(1.02);
        }

        .btn-home-secondary {
            background: rgba(255, 255, 255, 0.05);
            backdrop-filter: blur(10px);
            -webkit-backdrop-filter: blur(10px);
            border: 1.5px solid rgba(255, 255, 255, 0.15);
            color: #ffffff;
        }

        .btn-home-secondary:hover {
            background: rgba(255, 255, 255, 0.1);
            border-color: rgba(255, 255, 255, 0.3);
            transform: translateY(-3px) scale(1.05);
            box-shadow: 0 8px 24px rgba(255, 255, 255, 0.1);
        }

        .btn-home span {
            position: relative;
            z-index: 1;
        }

        /* Responsive */
        @media (max-width: 968px) {
            .home-features {
                grid-template-columns: 1fr;
                gap: 20px;
            }
        }

        @media (max-width: 768px) {
            .home-title {
                font-size: 42px;
            }

            .home-subtitle {
                font-size: 18px;
                margin-bottom: 48px;
            }

            .home-logo-icon {
                width: 60px;
                height: 60px;
                font-size: 30px;
            }

            .home-actions {
                flex-direction: column;
            }

            .btn-home {
                width: 100%;
                max-width: 300px;
            }
        }
    </style>
{% endblock %}
